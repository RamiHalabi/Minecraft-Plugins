- When attempting to run this plugin, create a data.yml in main project folder. 
- Put plugin.yml in main project folder 
- When creating new maven project (plugin) make sure pom.xml is reading the right path

